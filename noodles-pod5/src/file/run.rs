// standard

// third party

// local
use crate::io::reader::{Local, Atomic};

mod internal {
    pub(crate) mod backend {
        use crate::io::reader::ConcurrencyMode;

        pub trait Run<M: ConcurrencyMode>{}

        pub struct RunCore<M: ConcurrencyMode>{
            concurrency_mode:M,
        }
    }
}

#[cfg(feature = "backend")]
pub(crate) use internal::backend;

pub type Run = internal::backend::RunCore<Local>;
pub type ConcurrentRun = internal::backend::RunCore<Atomic>;